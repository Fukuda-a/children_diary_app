<!DOCTYPE html>
<html lang="ja">
  
<head>
<title>健康状況</title>
</head>
<body>
  <%= link_to :back do %>
    <button>戻る</button>
  <% end %>
    <tr>
      <h1>健康状況</h1>
    </tr>
    <tr>
      <td>
        <%= form_with model: @children_diary, :local => true do |f| %>
          <div class="field">
            <%= f.collection_select(:children_id, @children, :id, :name)%>
            <%= f.date_field :date %>
            <%= f.text_area :health %>
          </div>
          <%= f.submit "保存" %>
        <% end %>
      </td>
    </tr>
     
     
  </table>
</body>
</html>